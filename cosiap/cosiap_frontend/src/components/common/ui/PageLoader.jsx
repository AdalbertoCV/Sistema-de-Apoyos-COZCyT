
export default function PageLoader() {
    return (
        <div className="fixed inset-0 flex items-center justify-center z-50 bg-gray-400 bg-opacity-55">
            <div className="border-white h-20 w-20 animate-spin rounded-full border-8 border-t-[var(--principal-f)]"/>
        </div>
    );
}